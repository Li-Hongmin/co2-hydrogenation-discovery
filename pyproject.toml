[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "co2-hydrogenation-discovery"
version = "0.1.0"
description = "Mechanism-first AI-assisted discovery workspace for CO2 hydrogenation catalysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
