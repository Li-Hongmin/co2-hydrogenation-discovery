# Drive provenance

The SHA-256 values in `drive_split_sha256.txt` were read from the connected Google Drive file `SHA256SUMS.txt` on 2026-08-21. The accompanying Drive extraction note states that the reconstructed split archive passed an `unzip -t` check for 11,327 entries. This repository does not independently recompute the 12 part hashes because the multi-gigabyte raw parts remain outside GitHub.
