# Next actions

## Immediate data gate

1. Generate a compact manifest of the 11,327-entry split ZIP stored in Google Drive; do not copy the raw 23.67 GiB archive into GitHub.
2. Rank and selectively extract files associated with Figure 6 / S1-S5, GC/MS time series, operando XRD, In/Zr XAS, and DFT vacancy/local-environment calculations.
3. First raw-data adjudication: determine whether S4 continues to reactivate under unchanged standard feed or reaches a persistent activity deficit after ensemble-average redox recovery.

## Experimental gate

If the S4 deficit disappears after kinetic stabilization, stop the persistent-memory headline and study slow reactivation kinetics. If it persists, compare regeneration paths at a matched operando redox endpoint and independently measure morphology/accessible area and surface chemistry.

## Computational gate

Do not train a broad ML potential yet. First pass the localized-Zr structural-survival gate, then run only three Stage-1 mechanism probes: H-pair thermodynamics, direct CO2 vacancy healing, and H2COOH scission. Expand to vacancy migration, H2 dissociation, interfaces, or MLIP sampling only when an earlier result provides an explicit trigger.
