# Project status — 2026-08-21

## GitHub

The repository is initialized on `main` and contains the mechanism-first research core: study design, competing hypotheses, decisive probes, staged DFT manifest, tests, CI, and the external-data boundary.

## Google Drive

The full experimental/DFT archive is present as a verified 12-part split ZIP. The Drive-side note reports 11,327 valid archive entries after reconstruction/testing. SHA-256 checksums are available for all parts.

The small derived central-directory manifest has not yet appeared in Drive. Generating that manifest is the immediate data-engineering step because it allows selective extraction without moving the full raw archive into GitHub.

## Scientific next result

The first raw-data adjudication will test whether S4 is still recovering under unchanged standard feed or has reached a persistent functionally distinct state after ensemble-average redox recovery. This result determines whether the project proceeds toward delayed-reactivation kinetics, morphology/path-dependent regeneration, or hidden surface-state chemistry.
